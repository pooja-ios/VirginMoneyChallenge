# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'VirginMoneyChallenge' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for VirginMoneyChallenge

  pod 'Alamofire', '~> 5.2'
  pod 'MBProgressHUD', '~> 1.2.0'
  pod 'SDWebImage', '~> 5.0'

  target 'VirginMoneyChallengeTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'Nimble', '~> 9.0.0'
    pod 'Quick', '~> 3.1.2'
  end

  target 'VirginMoneyChallengeUITests' do
    # Pods for testing
    pod 'Nimble', '~> 9.0.0'
    pod 'Quick', '~> 3.1.2'
  end

end
